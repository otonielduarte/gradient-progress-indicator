name: gradient_progress_indicator
description: This package shows a circular progress indicator with gradient colors, and it is possible insert texts inside that.
version: 1.0.0+5
repository: https://github.com/otonielduarte/gradient-progress-indicator

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: